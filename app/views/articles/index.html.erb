<p id="notice"><%= notice %></p>

<%= cl_image_tag("yhahcp8c36y2rp46q0x6euoc0aeg", width: 200, height: 200, crop: :fill) %>

<%= cl_image_tag("qhkmxog6chpjmxgabio6",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>
<h1>Articles</h1>

<% @articles.each do |article| %>
    <% if article.photos.attached? %>
    <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path article.photo.key %>')">
      <td><%= article.title %></td>
    </div>
    <% end %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.body %></td>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article', new_article_path %>
